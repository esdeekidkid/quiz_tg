# Quiz Helper - Поддержка Moodle тестов с анализом лекций

Приложение для автоматического сопоставления ответов тестов Moodle с текстом лекций, оптимизированное для Render Free (512 МБ RAM).

## 🎯 Функциональность

- **Загрузка PDF лекций** - Экстрактирует текст из PDF файлов
- **Парсинг HTML тестов** - Анализирует структуру Moodle тестов
- **Автоматическое сопоставление** - Находит ответы в лекции
- **Визуализация результатов** - Показывает правильные ответы зеленым
- **Модальные фрагменты** - Кликните на ответ для просмотра подтверждающего текста из лекции

## 🚀 Быстрый старт

### Локальное развертывание

```bash
# Установка зависимостей
pip install -r requirements.txt

# Запуск приложения
uvicorn main:app --reload --port 8000

# Открыть браузер
http://localhost:8000
```

### Развертывание на Render

1. Клонируйте репозиторий
2. Подключитесь к Render и выберите "Web Service"
3. Выберите ветку и нажмите "Deploy"
4. Задайте переменные окружения:
   - `VITE_SUPABASE_URL` - URL вашего Supabase проекта
   - `VITE_SUPABASE_ANON_KEY` - Anon ключ Supabase

## 📋 Требования

- Python 3.11+
- Supabase аккаунт (бесплатный уровень подходит)
- 512 МБ RAM (Render Free)

## 📦 Архитектура

```
┌─────────────┐
│   Браузер   │
├─────────────┤
│   FastAPI   │◄─────┐
├─────────────┤      │
│  Supabase   │      │
└─────────────┘      │
                  Edge Functions
                   (TypeScript)
```

## 🔧 Компоненты

### Backend (Python)
- **main.py** - FastAPI приложение с эндпоинтами API
- Поддерживаемые типы вопросов:
  - Single choice (один правильный ответ)
  - Multiple choice (несколько правильных ответов)
  - Short answer (открытый вопрос)
  - Units (единицы измерения)

### Frontend (HTML/CSS/JS)
- Встроенный modal для фрагментов лекции
- Зеленая подсветка правильных ответов
- Адаптивная верстка для мобильных устройств

### Storage (Supabase)
- `quiz_sessions` - Хранит текст лекций с TTL 24h
- `quiz_results` - Кэш результатов обработки

### Serverless (Edge Functions)
- `quiz_storage` - Безопасное сохранение и чтение данных

## 📊 API Endpoints

### `GET /`
Главная страница приложения

### `POST /api/extract-text-from-pdf/`
Загрузка и парсинг PDF файла
- **Input**: multipart/form-data с файлом PDF
- **Output**: `{ session_id, length, snippet }`

### `POST /api/parse-quiz-html/`
Парсинг HTML теста Moodle
- **Input**: `{ html: string }`
- **Output**: `{ questions: [...] }`

### `POST /api/process-quiz/`
Обработка вопросов и сопоставление с лекцией
- **Input**: `{ questions: [...], session_id: string }`
- **Output**: `{ results: [...] }`

## 🎨 Типы вопросов и результаты

### Множественный выбор
```json
{
  "question": "Что такое ...",
  "type": "single",
  "options": [
    {
      "option": "Ответ 1",
      "is_correct": true,
      "best_snippet": "..."
    }
  ]
}
```

### Открытый вопрос
```json
{
  "question": "Что такое ...",
  "type": "short",
  "answer": "Найденный ответ",
  "is_correct": true,
  "best_snippet": "..."
}
```

## 💾 Использование памяти

| Компонент | Память |
|-----------|--------|
| Python runtime | ~100 МБ |
| FastAPI + deps | ~50 МБ |
| Буферы данных | ~30 МБ |
| LRU кэши | ~20 МБ |
| Gunicorn | ~20 МБ |
| **Итого** | **~220 МБ** |

## ⚙️ Конфигурация

### Ограничения
- Максимальный размер PDF: 10 МБ
- Максимальный размер фрагмента: 500 символов
- TTL сессий: 24 часа
- LRU кэш: максимум 10 элементов

### Параметры для оптимизации
В файле `main.py`:
- `excerpt_around(text, idx, length=500)` - размер фрагмента
- `@lru_cache(maxsize=10)` - размер кэша
- `if len(content) > 10 * 1024 * 1024` - лимит PDF

## 🐛 Troubleshooting

### 404 ошибки при process-quiz
- Проверьте, что sessionId передан в запросе
- Убедитесь, что Edge Function quiz_storage активна

### Высокое использование памяти
- Уменьшите размер входящих PDF
- Проверьте размер LRU кэша
- Перезагрузите приложение

### Медленная обработка
- Используйте PDF < 5 МБ
- Ограничьте количество вопросов < 100

## 📝 Примеры использования

### 1. Загрузка PDF
```javascript
const formData = new FormData();
formData.append('file', pdfFile);
const response = await fetch('/api/extract-text-from-pdf/', {
  method: 'POST',
  body: formData
});
const data = await response.json();
const sessionId = data.session_id;
```

### 2. Парсинг HTML
```javascript
const response = await fetch('/api/parse-quiz-html/', {
  method: 'POST',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify({ html: htmlString })
});
const { questions } = await response.json();
```

### 3. Обработка вопросов
```javascript
const response = await fetch('/api/process-quiz/', {
  method: 'POST',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify({ 
    questions: questions,
    session_id: sessionId 
  })
});
const { results } = await response.json();
```

## 📚 Структура проекта

```
.
├── main.py                    # Backend FastAPI
├── requirements.txt           # Python зависимости
├── Procfile                  # Render конфигурация
├── render.yaml               # Render YAML конфиг
├── templates/
│   └── index.html           # Фронтенд (HTML+JS+CSS)
├── static/
│   └── style.css            # Стили
├── supabase/
│   └── migrations/          # SQL миграции
└── README.md                # Этот файл
```

## 🔐 Безопасность

- RLS политики на всех таблицах Supabase
- Все данные в защищенной БД, не в памяти приложения
- Edge Functions используют Supabase JWT
- CORS заголовки правильно настроены

## 📈 Производительность

- Среднее время обработки: 2-5 секунд
- Поддержка до 100 вопросов в тесте
- Автоматическая очистка данных через 24 часа

## 🚀 Future Improvements

- [ ] Добавить поддержку Excel/CSV файлов
- [ ] Реализовать кэширование результатов на уровне браузера
- [ ] Добавить статистику использования
- [ ] Поддержка нескольких языков

## 📄 Лицензия

MIT License

## 👨‍💻 Разработано

Quiz Helper v1.0 - 2025-11-27

---

**Статус**: ✅ Готово к использованию
**Язык**: Русский
**Platform**: Render Free (512 MB RAM)
